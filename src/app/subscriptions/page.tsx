import { Subscriptions } from "@/components";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Mexpenses - Planos'
};

export default function PageSubscriptions () {
    return (
        <Subscriptions/>
    )
}